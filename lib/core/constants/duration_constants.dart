class AppDurationConstants {
  AppDurationConstants._();

  static const defaultSnackBarDuration = Duration(seconds: 3);
  static const delayMessageReaction = Duration(milliseconds: 500);
  static const delaySendTypingIndicator = Duration(milliseconds: 500);
  static const delayStopTypingIndicator = Duration(seconds: 5);
  static const delaySearchUser = Duration(milliseconds: 300);
  static const lastSeenUpdateDuration = Duration(seconds: 10);
}

class ApiDurationConstants {
  ApiDurationConstants._();

  static const reJoinChannelDuration = Duration(seconds: 5);
}
